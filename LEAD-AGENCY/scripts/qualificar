#!/usr/bin/env bash
# Verifica o site, recalcula o score e promove os leads acima do corte.
#   scripts/qualificar --corte 60
source "$(dirname "${BASH_SOURCE[0]}")/_comum.sh"
exec "$PY" -m app.cli qualificar "$@"
